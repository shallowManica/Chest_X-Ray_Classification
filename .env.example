# Example environment configuration
# Copy this file to .env and update with your local paths
# All paths are optional - the project will use relative paths by default

# Path to raw data directory (should contain train/val/test subdirectories)
# Default: ./data/raw/archive
# DATA_DIR=/path/to/your/data/raw/archive

# Path to processed data directory
# Default: ./data/processed
# PROCESSED_DATA_DIR=/path/to/your/data/processed

# Path to models directory (where trained models are saved)
# Default: ./models
# MODELS_DIR=/path/to/your/models

# Path to figures directory (where plots and visualizations are saved)
# Default: ./reports/figures
# FIGURES_DIR=/path/to/your/reports/figures
